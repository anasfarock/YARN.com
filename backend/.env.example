# Environment Configuration Setup
# 
# Steps for Contributors:
# 1. Rename the '.env.example' file to '.env'
# 2. Open MongoDB Compass and connect to your MongoDB instance
# 3. Copy your MongoDB connection URI from MongoDB Compass
# 4. Replace the placeholder MongoDB URI in the .env file with your own connection string
# 5. Ensure all other environment variables are configured according to your local setup
#
# Note: Never commit the .env file to version control as it contains sensitive information

MONGODB_URI=Place_Your_MONGODB_URI
PORT=5000
